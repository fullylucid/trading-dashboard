[Unit]
Description=Evaluate chart-condition smart alerts every 5 minutes (market hours-ish)

[Timer]
# Every 5 minutes. The evaluator is cheap and dedups per bar, so off-hours runs are no-ops.
OnCalendar=*-*-* *:0/5
Persistent=false

[Install]
WantedBy=timers.target
